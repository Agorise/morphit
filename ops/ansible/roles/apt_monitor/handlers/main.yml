# roles/apt_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-apt-monitor-timer
  ansible.builtin.systemd:
    name: morphit-apt-monitor.timer
    state: restarted
