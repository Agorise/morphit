# roles/postfix_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-postfix-monitor-timer
  ansible.builtin.systemd:
    name: morphit-postfix-monitor.timer
    state: restarted
