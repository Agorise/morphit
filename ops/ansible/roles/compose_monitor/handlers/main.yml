# roles/compose_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-compose-monitor-timer
  ansible.builtin.systemd:
    name: morphit-compose-monitor.timer
    state: restarted
