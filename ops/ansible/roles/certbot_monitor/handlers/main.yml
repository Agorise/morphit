# roles/certbot_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-certbot-monitor-timer
  ansible.builtin.systemd:
    name: morphit-certbot-monitor.timer
    state: restarted
