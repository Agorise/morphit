# roles/host_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-host-monitor-timer
  ansible.builtin.systemd:
    name: morphit-host-monitor.timer
    state: restarted
