# roles/trivy_monitor/handlers/main.yml
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart morphit-trivy-monitor-timer
  ansible.builtin.systemd:
    name: morphit-trivy-monitor.timer
    state: restarted
