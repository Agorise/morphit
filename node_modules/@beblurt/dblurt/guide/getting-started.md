# Getting started with dblurt

This guide gets a Node.js project connected to Blurt in a few minutes.

## 1. Install

```bash
npm install @beblurt/dblurt
```

`@beblurt/dblurt` declares its Node.js runtime requirement in `package.json`; see [runtime-compatibility.md](./runtime-compatibility.md) for the current contract.

## 2. Create a client

```js
const { Client } = require('@beblurt/dblurt');

const rpcList = [
    'https://rpc.blurt.blog',
    'https://blurt-rpc.saboin.com',
    'https://rpc.beblurt.com',
    'https://blurtrpc.dagobert.uk',
    'https://rpc.drakernoise.com'
];

const client = new Client(rpcList, {
    timeout: 15_000,
    failoverThreshold: 3
});
```

The endpoint list is used by the built-in failover system. See [failover.md](./failover.md).

## 3. Read chain properties

```js
async function main() {
    const props = await client.condenser.getDynamicGlobalProperties();
    console.log({
        head: props.head_block_number,
        irreversible: props.last_irreversible_block_num,
        time: props.time
    });
}

main().catch(console.error);
```

## 4. Load an account

```js
const accounts = await client.condenser.getAccounts(['beblurt']);
const account = accounts[0];

if (account) {
    console.log(account.name);
    console.log(account.balance);
    console.log(account.vesting_shares);
}
```

## 5. Query social data through Nexus Layer 2

```js
const posts = await client.nexus.getRankedPosts('trending', null, null, 10, 'blurt');

for (const post of posts) {
    console.log(`${post.author}/${post.permlink}: ${post.title}`);
}
```

For the Layer 1 / Nexus distinction, see [Blockchain model](./BLOCKCHAIN_MODEL.md).

## 6. Where to go next

- Runnable examples: [examples.md](./examples.md) and [`../examples/README.md`](../examples/README.md)
- Browser usage: [browser.md](./browser.md)
- TypeScript: [typescript.md](./typescript.md)
- Broadcasting: [broadcast.md](./broadcast.md)
- API map: [api-guide.md](./api-guide.md)
