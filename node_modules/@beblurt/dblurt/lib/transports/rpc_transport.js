"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateRpcResponse = void 0;
const errors_1 = require("../errors");
const validateRpcResponse = (response) => {
    if (!response || typeof response !== 'object' || Array.isArray(response)) {
        throw new errors_1.ValidationError('Malformed JSON-RPC response: expected object');
    }
    if ('error' in response && response.error !== undefined && (response.error === null || typeof response.error !== 'object' || Array.isArray(response.error))) {
        throw new errors_1.ValidationError('Malformed JSON-RPC response: error must be an object');
    }
    if (!('result' in response) && !('error' in response)) {
        throw new errors_1.ValidationError('Malformed JSON-RPC response: missing result or error');
    }
    return response;
};
exports.validateRpcResponse = validateRpcResponse;
