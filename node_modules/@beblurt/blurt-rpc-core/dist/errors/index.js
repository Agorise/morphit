/** Base class for all public errors thrown by the core. */
export class RpcErrorBase extends Error {
    category;
    endpoint;
    method;
    requestId;
    attempt;
    elapsedMs;
    retryable;
    failoverable;
    status;
    code;
    data;
    cause;
    constructor(message, details) {
        super(message, { cause: details.cause });
        this.name = new.target.name;
        this.category = details.category;
        this.endpoint = details.endpoint;
        this.method = details.method;
        this.requestId = details.requestId;
        this.attempt = details.attempt;
        this.elapsedMs = details.elapsedMs;
        this.retryable = details.retryable ?? false;
        this.failoverable = details.failoverable ?? false;
        this.status = details.status;
        this.code = details.code;
        this.data = details.data;
        this.cause = details.cause;
    }
}
/** JSON-RPC error returned by a remote endpoint. */
export class RpcError extends RpcErrorBase {
    rpc;
    constructor(rpc, details = {}) {
        super(rpc.message, {
            ...details,
            category: 'rpc_error',
            code: rpc.code,
            data: rpc.data,
            retryable: details.retryable ?? false,
            failoverable: details.failoverable ?? false
        });
        this.rpc = rpc;
    }
}
/** Network-level failure such as DNS, socket or TLS failure. */
export class RpcNetworkError extends RpcErrorBase {
    constructor(message, details = {}) {
        super(message, { ...details, category: 'network', retryable: details.retryable ?? true, failoverable: details.failoverable ?? true });
    }
}
/** Local timeout. */
export class RpcTimeoutError extends RpcErrorBase {
    constructor(message = 'RPC request timed out', details = {}) {
        super(message, { ...details, category: 'timeout', retryable: details.retryable ?? true, failoverable: details.failoverable ?? true });
    }
}
/** Explicit abort via AbortSignal. */
export class RpcAbortError extends RpcErrorBase {
    constructor(message = 'RPC request was aborted', details = {}) {
        super(message, { ...details, category: 'abort', retryable: details.retryable ?? false, failoverable: details.failoverable ?? false });
    }
}
/** HTTP non-OK response. */
export class RpcHttpError extends RpcErrorBase {
    constructor(message, details = {}) {
        const retryable = details.retryable ?? (details.status === undefined ? false : details.status >= 500);
        super(message, { ...details, category: 'http', retryable, failoverable: details.failoverable ?? retryable });
    }
}
/** HTTP 429 or equivalent rate-limit signal. */
export class RpcRateLimitError extends RpcErrorBase {
    retryAfterMs;
    constructor(message = 'RPC endpoint rate limited the request', details = {}) {
        super(message, { ...details, category: 'rate_limit', status: details.status ?? 429, retryable: details.retryable ?? true, failoverable: details.failoverable ?? true });
        this.retryAfterMs = details.retryAfterMs;
    }
}
/** Protocol-level failure before a valid JSON-RPC response can be used. */
export class RpcProtocolError extends RpcErrorBase {
    constructor(message, details = {}) {
        super(message, { ...details, category: 'protocol', retryable: details.retryable ?? false, failoverable: details.failoverable ?? false });
    }
}
/** Invalid JSON, invalid shape or id mismatch. */
export class RpcInvalidResponseError extends RpcErrorBase {
    constructor(message, details = {}) {
        super(message, { ...details, category: 'invalid_response', retryable: details.retryable ?? true, failoverable: details.failoverable ?? true });
    }
}
/** Serialization or parsing failure. */
export class RpcSerializationError extends RpcErrorBase {
    constructor(message, details = {}) {
        super(message, { ...details, category: 'serialization', retryable: details.retryable ?? false, failoverable: details.failoverable ?? false });
    }
}
/** All retry/failover attempts have been exhausted. */
export class RpcFailoverExhaustedError extends RpcErrorBase {
    errors;
    constructor(errors, details = {}) {
        super('RPC failover attempts exhausted', { ...details, category: 'failover_exhausted', retryable: false, failoverable: false, cause: errors[errors.length - 1] });
        this.errors = errors;
    }
}
/** Error used when a caller requests an unsupported generic capability. */
export class RpcUnsupportedError extends RpcErrorBase {
    constructor(feature, details = {}) {
        super(`${feature} is not supported`, { ...details, category: 'unsupported' });
    }
}
/** @deprecated Use RpcUnsupportedError. */
export const NotImplementedError = RpcUnsupportedError;
//# sourceMappingURL=index.js.map