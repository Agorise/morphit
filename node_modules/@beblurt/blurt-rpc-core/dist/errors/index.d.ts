import type { JsonRpcErrorObject, JsonRpcId, RpcEndpoint, RpcMethod } from '../types/index.js';
/** Stable category for a public RPC error. */
export type RpcErrorCategory = 'rpc_error' | 'network' | 'timeout' | 'abort' | 'http' | 'rate_limit' | 'protocol' | 'invalid_response' | 'serialization' | 'failover_exhausted' | 'policy_rejected' | 'unsupported' | 'unknown';
/** Details attached to every RPC error. */
export interface RpcErrorDetails {
    category: RpcErrorCategory;
    endpoint?: RpcEndpoint;
    method?: RpcMethod;
    requestId?: JsonRpcId;
    attempt?: number;
    elapsedMs?: number;
    retryable?: boolean;
    failoverable?: boolean;
    status?: number;
    code?: number;
    data?: unknown;
    cause?: unknown;
}
/** Base class for all public errors thrown by the core. */
export declare class RpcErrorBase extends Error {
    readonly category: RpcErrorCategory;
    readonly endpoint: RpcEndpoint | undefined;
    readonly method: RpcMethod | undefined;
    readonly requestId: JsonRpcId | undefined;
    readonly attempt: number | undefined;
    readonly elapsedMs: number | undefined;
    readonly retryable: boolean;
    readonly failoverable: boolean;
    readonly status: number | undefined;
    readonly code: number | undefined;
    readonly data: unknown;
    readonly cause: unknown;
    constructor(message: string, details: RpcErrorDetails);
}
/** JSON-RPC error returned by a remote endpoint. */
export declare class RpcError extends RpcErrorBase {
    readonly rpc: JsonRpcErrorObject;
    constructor(rpc: JsonRpcErrorObject, details?: Omit<RpcErrorDetails, 'category' | 'code' | 'data'>);
}
/** Network-level failure such as DNS, socket or TLS failure. */
export declare class RpcNetworkError extends RpcErrorBase {
    constructor(message: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** Local timeout. */
export declare class RpcTimeoutError extends RpcErrorBase {
    constructor(message?: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** Explicit abort via AbortSignal. */
export declare class RpcAbortError extends RpcErrorBase {
    constructor(message?: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** HTTP non-OK response. */
export declare class RpcHttpError extends RpcErrorBase {
    constructor(message: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** HTTP 429 or equivalent rate-limit signal. */
export declare class RpcRateLimitError extends RpcErrorBase {
    readonly retryAfterMs: number | undefined;
    constructor(message?: string, details?: Omit<RpcErrorDetails, 'category'> & {
        retryAfterMs?: number;
    });
}
/** Protocol-level failure before a valid JSON-RPC response can be used. */
export declare class RpcProtocolError extends RpcErrorBase {
    constructor(message: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** Invalid JSON, invalid shape or id mismatch. */
export declare class RpcInvalidResponseError extends RpcErrorBase {
    constructor(message: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** Serialization or parsing failure. */
export declare class RpcSerializationError extends RpcErrorBase {
    constructor(message: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** All retry/failover attempts have been exhausted. */
export declare class RpcFailoverExhaustedError extends RpcErrorBase {
    readonly errors: readonly RpcErrorBase[];
    constructor(errors: readonly RpcErrorBase[], details?: Omit<RpcErrorDetails, 'category' | 'cause'>);
}
/** Error used when a caller requests an unsupported generic capability. */
export declare class RpcUnsupportedError extends RpcErrorBase {
    constructor(feature: string, details?: Omit<RpcErrorDetails, 'category'>);
}
/** @deprecated Use RpcUnsupportedError. */
export declare const NotImplementedError: typeof RpcUnsupportedError;
//# sourceMappingURL=index.d.ts.map