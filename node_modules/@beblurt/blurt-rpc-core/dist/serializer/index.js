/** Default JSON.stringify based serializer. */
export function jsonSerializer(options = {}) {
    return {
        stringify(value) {
            return JSON.stringify(value, options.replacer);
        }
    };
}
//# sourceMappingURL=index.js.map