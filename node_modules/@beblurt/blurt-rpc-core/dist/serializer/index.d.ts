import type { RpcContext } from '../types/index.js';
/** Converts an RPC payload to a wire string. */
export interface RpcSerializer {
    stringify(value: unknown, context: RpcContext): string;
}
/** Options for the default JSON serializer. */
export interface JsonSerializerOptions {
    replacer?: (this: unknown, key: string, value: unknown) => unknown;
}
/** Default JSON.stringify based serializer. */
export declare function jsonSerializer(options?: JsonSerializerOptions): RpcSerializer;
//# sourceMappingURL=index.d.ts.map