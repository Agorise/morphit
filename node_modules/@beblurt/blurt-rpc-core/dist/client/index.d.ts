import type { JsonRpcRequest, JsonRpcResponse, RpcBatchCall, RpcBatchItemResult, RpcBatchOptions, RpcCallOptions, RpcClientOptions, RpcEndpointInput, RpcEndpointState, RpcMethod } from '../types/index.js';
/** Compose two JSON-RPC method segments without assigning domain semantics. */
export declare function composeMethod(namespace: string, method: string): string;
/** Create a new RpcClient. */
export declare function createRpcClient(endpoints: RpcEndpointInput | readonly RpcEndpointInput[], options?: RpcClientOptions): RpcClient;
/** Main facade for generic JSON-RPC calls. */
export declare class RpcClient {
    readonly endpoints: RpcEndpointInput | readonly RpcEndpointInput[];
    readonly options: RpcClientOptions;
    private readonly transport;
    private readonly nodePool;
    private readonly hooks;
    private readonly cacheOptions;
    constructor(endpoints: RpcEndpointInput | readonly RpcEndpointInput[], options?: RpcClientOptions);
    /** Execute one JSON-RPC call and return its result by default. */
    call<TResult = unknown, TParams = unknown>(method: RpcMethod, params?: TParams, options?: RpcCallOptions<TResult>): Promise<TResult>;
    /** Execute a prebuilt JSON-RPC request and return the raw response. */
    callRaw<TResult = unknown, TParams = unknown>(request: JsonRpcRequest<TParams>, options?: RpcCallOptions<TResult>): Promise<JsonRpcResponse<TResult>>;
    /** Compose namespace + method then execute a JSON-RPC call. */
    callMethod<TResult = unknown, TParams = unknown>(namespace: string, method: string, params?: TParams, options?: RpcCallOptions<TResult>): Promise<TResult>;
    /** Execute a JSON-RPC notification. */
    notify<TParams = unknown>(method: RpcMethod, params?: TParams, options?: Omit<RpcCallOptions<void>, 'id' | 'responseMode'>): Promise<void>;
    /** Execute a JSON-RPC batch call and preserve item-level results. */
    callBatch(calls: readonly RpcBatchCall[], options?: RpcBatchOptions): Promise<readonly RpcBatchItemResult[]>;
    /** Return the current endpoint state when a NodePool is configured. */
    getCurrentEndpoint(): RpcEndpointState | undefined;
    /** Return endpoint states when a NodePool is configured. */
    getEndpointStates(): readonly RpcEndpointState[];
    /** Replace the endpoint list on a configured NodePool. */
    setEndpoints(endpoints: readonly RpcEndpointInput[]): void;
    /** Close owned resources such as transports. */
    close(): Promise<void>;
    private createRequest;
    private createContext;
    private executeWithRetry;
    private resolveRetryPolicy;
    private transportContext;
    private getCacheKey;
    private classifyError;
    private metrics;
    private metricEvent;
    private errorDetails;
    private failoverDetails;
    private recordMetric;
    private runBeforeRetry;
    private runAfterAttempt;
    private runFailover;
    private runOnError;
}
//# sourceMappingURL=index.d.ts.map