import type { RpcErrorBase } from '../errors/index.js';
import type { RetryDecision } from '../retry/index.js';
import type { JsonRpcPayload, JsonRpcRequest, JsonRpcResponse, RpcAttemptContext, RpcAttemptOutcome, RpcContext, RpcEndpointState } from '../types/index.js';
/** Public hooks. Hooks are ordered callbacks; metrics remain a separate channel. */
export interface RpcHooks {
    beforeRequest?: RpcHookBeforeRequest | readonly RpcHookBeforeRequest[];
    afterResponse?: RpcHookAfterResponse | readonly RpcHookAfterResponse[];
    beforeRetry?: RpcHookBeforeRetry | readonly RpcHookBeforeRetry[];
    afterAttempt?: RpcHookAfterAttempt | readonly RpcHookAfterAttempt[];
    onFailover?: RpcHookFailover | readonly RpcHookFailover[];
    onError?: RpcHookError | readonly RpcHookError[];
}
/** Hook called before a request reaches the transport. */
export type RpcHookBeforeRequest = (request: JsonRpcPayload | readonly JsonRpcRequest[], context: RpcContext) => Promise<JsonRpcPayload | readonly JsonRpcRequest[] | void> | JsonRpcPayload | readonly JsonRpcRequest[] | void;
/** Hook called after a valid transport response. */
export type RpcHookAfterResponse = (response: JsonRpcResponse | readonly JsonRpcResponse[], context: RpcContext) => Promise<JsonRpcResponse | readonly JsonRpcResponse[] | void> | JsonRpcResponse | readonly JsonRpcResponse[] | void;
/** Hook called before sleeping/retrying. */
export type RpcHookBeforeRetry = (error: RpcErrorBase, decision: RetryDecision, context: RpcAttemptContext) => Promise<void> | void;
/** Hook called after each attempt. */
export type RpcHookAfterAttempt = (outcome: RpcAttemptOutcome, context: RpcAttemptContext) => Promise<void> | void;
/** Hook called when a retry selects a different endpoint. */
export type RpcHookFailover = (from: RpcEndpointState, to: RpcEndpointState | undefined, error: RpcErrorBase, context: RpcAttemptContext) => Promise<void> | void;
/** Hook called when an operation fails. */
export type RpcHookError = (error: RpcErrorBase, context: RpcContext) => Promise<void> | void;
//# sourceMappingURL=index.d.ts.map