import type { RpcErrorBase } from '../errors/index.js';
import type { RpcAttemptContext } from '../types/index.js';
/** Computes the delay before the next retry attempt. */
export interface BackoffStrategy {
    delayMs(attempt: number, error: RpcErrorBase, context: RpcAttemptContext): number;
}
/** Constant delay backoff. */
export declare function fixedBackoff(delayMs: number): BackoffStrategy;
/** Exponential delay backoff. */
export declare function exponentialBackoff(options?: {
    baseDelayMs?: number;
    maxDelayMs?: number;
    factor?: number;
    jitterRatio?: number;
    random?: () => number;
}): BackoffStrategy;
/** Add symmetric random jitter to an existing backoff. */
export declare function jitteredBackoff(base: BackoffStrategy, options?: {
    jitterRatio?: number;
    random?: () => number;
}): BackoffStrategy;
//# sourceMappingURL=index.d.ts.map