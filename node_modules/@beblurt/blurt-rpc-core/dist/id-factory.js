/** Create an incrementing numeric JSON-RPC id factory. */
export function incrementalIdFactory(start = 1) {
    let current = start;
    return {
        next() {
            return current++;
        },
        reset() {
            current = start;
        }
    };
}
/** Create a random string JSON-RPC id factory. */
export function randomStringIdFactory(options = {}) {
    const prefix = options.prefix ?? '';
    const length = options.length ?? 12;
    return {
        next() {
            return `${prefix}${Math.random().toString(36).slice(2, 2 + length)}`;
        }
    };
}
//# sourceMappingURL=id-factory.js.map