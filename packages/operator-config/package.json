{
  "name": "@morphit/operator-config",
  "version": "1.12.1",
  "private": true,
  "type": "module",
  "description": "Loads morphit.config.env and projects its values into process.env for the indexer and relay to consume.",
  "license": "AGPL-3.0-or-later",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
