{
	"name": "@morphit/release-schema",
	"version": "1.4.10",
	"private": true,
	"type": "module",
	"description": "Shared morphit_release_v1 op schema (types) + validator. The frontend (apps/web) and the indexer (its release-handler parity test, release-build and release-validator scripts) both import from here, so the validator is one canonical source of truth instead of the indexer reaching across into apps/web source.",
	"license": "AGPL-3.0-only",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src"
	],
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "^5.6.3"
	}
}
